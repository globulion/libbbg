#!/usr/bin/env python
"""
Greek letters

Version 1.0
"""
# lower-cases
alpha   = chr(0x3B1).encode('utf-8')
beta    = chr(0x3B2).encode('utf-8')
gamma   = chr(0x3B3).encode('utf-8')
delta   = chr(0x3B4).encode('utf-8')
epsilon = chr(0x3B5).encode('utf-8')
omega   = chr(0x3c9).encode('utf-8')

# upper-cases
Alpha   = chr(0x391).encode('utf-8')
Beta    = chr(0x392).encode('utf-8')
Gamma   = chr(0x393).encode('utf-8')
Delta   = chr(0x394).encode('utf-8')
Epsilon = chr(0x395).encode('utf-8')
